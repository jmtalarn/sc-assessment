import React from 'react';
import styled from 'styled-components';

const H1 = styled.h1`
  font-family: 'Press Start 2P', cursive;
`;

const Title = () => <H1>Atari 2600 games datatable</H1>;

export default Title;
